# Passwords should be injected via environment variables or secrets management
port 6379
requirepass ${REDIS_PASSWORD}
replicaof redis-master 6379
masterauth ${REDIS_PASSWORD}
appendonly yes
maxmemory-policy noeviction
