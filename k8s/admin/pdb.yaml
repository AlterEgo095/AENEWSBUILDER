apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: admin-pdb
  namespace: aenews-builder
  labels:
    app: admin
    component: frontend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: admin
      component: frontend
