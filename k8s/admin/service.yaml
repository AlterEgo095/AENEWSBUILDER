apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: aenews-builder
  labels:
    app: admin
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: admin
    component: frontend
