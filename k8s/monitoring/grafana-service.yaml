apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: aenews-builder
  labels:
    app: grafana
    component: monitoring
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: grafana
    component: monitoring
