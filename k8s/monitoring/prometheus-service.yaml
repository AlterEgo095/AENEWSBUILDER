apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: aenews-builder
  labels:
    app: prometheus
    component: monitoring
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: prometheus
    component: monitoring
