apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: aenews-builder
  labels:
    app: api
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: api
    component: backend
