apiVersion: v1
kind: Service
metadata:
  name: redis-replica
  namespace: aenews-builder
  labels:
    app: redis
    component: replica
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app: redis
    component: replica
