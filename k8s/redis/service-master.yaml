apiVersion: v1
kind: Service
metadata:
  name: redis-master
  namespace: aenews-builder
  labels:
    app: redis
    component: master
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    app: redis
    component: master
