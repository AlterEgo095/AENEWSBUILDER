apiVersion: v1
kind: Service
metadata:
  name: studio-service
  namespace: aenews-builder
  labels:
    app: studio
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: studio
    component: frontend
